## 1. Documentation contract

- [x] 1.1 Add the `project-documentation` baseline specification from the approved
  delta specification.
- [x] 1.2 Verify that the README contains the `Operational behavior` section and
  the roadmap referenced by the new requirement.

## 2. Validation

- [x] 2.1 Run `openspec validate --all` after synchronizing the baseline
  specification.
